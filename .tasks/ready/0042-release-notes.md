# Release Notes — Task 0042

## Added
- `great status` now shows a hint when issues are detected: "Run `great doctor` for exit-code health checks in CI."
- The hint appears only in human-readable mode when tools, MCP commands, or secrets are missing
- Clean environments and JSON mode are unaffected
