# /discover — UX Discovery Sweep

$ARGUMENTS

Deming runs sequential subagents: **Nielsen** (Sonnet, full exploratory sweep + Playwright) -> **Nightingale** (Sonnet, triage to `.tasks/backlog/`). No fixes, no code changes.
